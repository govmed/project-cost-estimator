/**
 * Vitest global setup.
 *
 * Loaded by vite.config.ts via test.setupFiles.
 * Extends expect() with @testing-library/jest-dom matchers when running in jsdom.
 */
import '@testing-library/jest-dom/vitest';
